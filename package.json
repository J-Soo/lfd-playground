{
  "name": "lfd-playground",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "description": "Multi-game platform with realtime features",
  "scripts": {
    "dev": "npm run build:packages && turbo run dev",
    "build": "npm run build:packages && turbo run build",
    "build:packages": "npm run build --workspace=packages/shared-utils && npm run build --workspace=packages/supabase-client && npm run build --workspace=packages/game-core && npm run build --workspace=packages/ui-kit",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\""
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@typescript-eslint/eslint-plugin": "^6.19.0",
    "@typescript-eslint/parser": "^6.19.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.4",
    "turbo": "^1.11.3",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}